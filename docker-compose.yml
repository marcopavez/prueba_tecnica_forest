services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
      DB_PATH: /data/bike_rental.db
      JWT_SECRET: change-me-in-production
      ADMIN_CREDENTIALS: YWRtaW46cGFzc3dvcmQ=
    volumes:
      - ./bike_rental.db:/data/bike_rental.db 

volumes:
  db_data:
