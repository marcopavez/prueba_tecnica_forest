version: "3.9"
services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
      DB_PATH: /data/bike_rental.db
      JWT_SECRET: change-me-in-production
      # admin:password
      ADMIN_CREDENTIALS: YWRtaW46cGFzc3dvcmQ=
    volumes:
      - db_data:/data

volumes:
  db_data:
